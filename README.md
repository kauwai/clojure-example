Simple repo that showcases clojure-lsp resolving leiningen.core.project/read-raw to leiningen-core jar instead of leiningen jar.

## Running
`clojure -M -m main -workspace ~/projetos/bela/clojure-example`